---
- include_tasks: mng_addons/main.yml
  tags:
    - k8s
    - addons
    - dashboard

- name: Run proxy command to serve dashboard
  command: kubectl proxy
  tags:
    - k8s
    - addons
    - dashboard

# Ensure checks are run
- include_tasks: checks/main.yml
  tags:
    - k8s
    - cluster-checks
